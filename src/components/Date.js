const Date = ({date}) => {
    return (
        <div 
            style={{
                display: 'flex',
                alignItems: 'center',
                justifyContent: 'center',
                width: '30%',
                height: '100%',
                border: '1px solid white',
                borderRadius: '8px',
                backgroundColor: '#191919'
            }}>
                {date}</div>
    )
}

export default Date